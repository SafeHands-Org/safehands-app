import 'package:flutter/material.dart';

class FamilyInviteView extends StatelessWidget {
  const FamilyInviteView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: []
      )
    );
  }
}